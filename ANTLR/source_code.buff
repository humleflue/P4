number fib(number x) =
    if(x == 0 || x == 1) return 1;
    return fib(x - 1) + fib(x - 2);
    endf

fib(20)?;

number shouldNotWork() =
    if(true) return true;
    if(false) return 2;
    return 2;
    endf